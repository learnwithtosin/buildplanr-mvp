# Base URL of the backend API (see docs/API.md).
# The frontend calls the backend over HTTPS/JSON only — it never talks to
# Postgres or OpenAI directly (see docs/Architecture.md).
NEXT_PUBLIC_API_URL=http://localhost:4000
